import React from 'react';
import styles from './create_group.module.css';

const CreateGroup = ({}) => {
    return (
        <h1 className={styles.title}>모임을 만들어주세요!</h1>
    );
}

export default CreateGroup;